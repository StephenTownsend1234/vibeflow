# Global profile — ~/.claude/vibeflow/playbook.md

<!--
Lives OUTSIDE the skill repo, shared across all the user's projects. Two sections.
/wrap maintains both; every command reads it at session start.
-->

## Working preferences

<!-- Edit in place, don't append. Max ~10 bullets. Updated by /wrap when the user
corrects HOW we work (verbosity, check-in rhythm, decision framing, scope appetite) —
observed corrections, not praise. Delete bullets that stop being true. -->

- <e.g. "Prefers silent execution — narrate decisions and tradeoffs, not operations">
- <e.g. "Technical founder: lean, technical briefs; no hand-holding">

## Lessons

<!-- Cross-project principles, positive or negative — not a diary. Merge-first: a new
lesson that shares a mechanism with an existing entry amends that entry in place
instead of adding a sibling. Keep the kernel + reuse-when; the war story stays in the
project's own files. This file loads into every session of every project — an entry
earns its place only if it would change how a session on a DIFFERENT project acts. -->

### <short principle title>
<the mechanism — what actually causes the pain or the win, in 2–4 lines>
Reuse when: <the situation to apply it again>
