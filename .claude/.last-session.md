2026-08-26 · global-playbook distill + merge-first pack fix (Freebuild)
Stephen flagged playbook bloat → distilled 291→74 lines / 23→14 principle entries (pre-distill version exists only in that chat's transcript — no git outside the repo); merge-first + the "different-project" bar encoded in CORE, wrap/SKILL.md, templates/global-profile.md.
Also reconciled + committed: the 08-03 roadmap correction (Jumbo v3 migration long done) that had sat uncommitted.
Answered: bootstrap's "weird text file" = Claude Code's oversized-tool-result spill file (tool-results/), harmless.
Sprint design-integration still 5/6 — human checkpoint open (Stephen reads the wiring diff, aa980ee).
Jumbo reminder (carried): capture the long-covid knowledge-base idea on JUMBO's roadmap next Jumbo session (sketch in research/gbrain-learnings.md).
Next per ROADMAP Now: onboard the brother.
